# name: CI
# on:
#   push:
#     branches: [ main ]
#   pull_request:

# jobs:
#   validate:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Terraform fmt (no init)
#         run: |
#           terraform fmt -check -recursive infra/terraform
#       - name: Kustomize build
#         run: |
#           sudo apt-get update && sudo apt-get install -y kustomize
#           kustomize build gitops/apps > /tmp/rendered.yaml
#       - name: Basic YAML check
#         run: |
#           python -c "import yaml,sys; yaml.safe_load_all(open('/tmp/rendered.yaml'))" || true
